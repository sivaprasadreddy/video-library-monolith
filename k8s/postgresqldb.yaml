apiVersion: v1
kind: Service
metadata:
  name: video-library-postgres-svc
spec:
  selector:
    app: video-library-postgres-pod
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-library-postgres-deployment
spec:
  selector:
    matchLabels:
      app: video-library-postgres-pod
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: video-library-postgres-pod
    spec:
      containers:
        - name: postgres
          image: "postgres:12.3"
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: video-library-secrets
                  key: postgres_username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: video-library-secrets
                  key: postgres_password
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: video-library-configmap
                  key: postgres_database